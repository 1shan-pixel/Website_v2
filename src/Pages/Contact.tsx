// components/About.js
import React, { useState } from "react";

const Contact = () => {
    const [activeRoute, setActiveRoute] = useState("");

    return (
        <div>
            This is the content for the content page.
        </div>
    );
};

export default Contact;
